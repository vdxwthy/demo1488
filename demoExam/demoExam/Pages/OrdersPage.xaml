<Page x:Class="demoExam.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Заказы"
      d:DesignHeight="450"
      d:DesignWidth="800">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <Button x:Name="BackButton"
                        Content="Назад"
                        HorizontalAlignment="Left"
                        Padding="10,2"
                        Click="BackButton_Click"/>
                <Button x:Name="AddOrderButton"
                        Content="Добавить заказ"
                        Margin="10,0,0,0"
                        Padding="10,2"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Click="AddOrderButton_Click"/>
            </StackPanel>

            <TextBlock Text="Список заказов"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,10,0,0"
                       HorizontalAlignment="Center"/>
        </DockPanel>

        <ListView x:Name="OrdersList"
                  Grid.Row="1"
                  Margin="10"
                  HorizontalContentAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black"
                            BorderThickness="1"
                            Margin="0"
                            MouseLeftButtonUp="OrderItemBorder_MouseLeftButtonUp">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="5">
                                <TextBlock Text="{Binding Code, StringFormat={}Артикул заказа: {0}}"
                                           FontWeight="Bold"
                                           FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Статус заказа: "/>
                                    <TextBlock Text="{Binding StatusOrder}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Адрес пункта выдачи: "/>
                                    <TextBlock Text="{Binding PickupAddress}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Дата заказа: "/>
                                    <TextBlock Text="{Binding DateOrder, StringFormat=dd.MM.yyyy}"/>
                                </StackPanel>
                            </StackPanel>

                            <Border Grid.Column="1"
                                    BorderBrush="Black"
                                    BorderThickness="1"
                                    Margin="10"
                                    Padding="10"
                                    VerticalAlignment="Center">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Дата выдачи"
                                               FontWeight="Bold"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DateDelivery, StringFormat=dd.MM.yyyy}"
                                               FontSize="16"
                                               HorizontalAlignment="Center"
                                               Margin="0,5,0,0"/>
                                    <Button Content="Удалить"
                                            Margin="0,10,0,0"
                                            Padding="8,2"
                                            Visibility="{Binding RelativeSource={RelativeSource AncestorType=Page}, Path=DataContext.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Click="DeleteOrderButton_Click"
                                            Tag="{Binding Id}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
